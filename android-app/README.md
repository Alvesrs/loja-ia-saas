# Loja IA Android

Aplicativo Android nativo em formato WebView seguro apontando para o painel de produção do Loja IA.

## Build
```bash
gradle :app:assembleDebug
```

APK gerado em `app/build/outputs/apk/debug/app-debug.apk`.
