{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "python3 -m zipfile -e loja-ia-saas-whatsapp-12a.zip . && echo '[FILE_LIST_BEGIN]' && find src public -maxdepth 4 -type f -print && echo '[FILE_LIST_END]' && node patches/fix-public-ia-route.js && node patches/log-ai-response-origin.js && node patches/stock-last-unit-policy.js && node patches/order-status-management.js && node patches/expose-plan-dashboard.js && node patches/meta-embedded-signup.js && npm ci --omit=dev"
  },
  "deploy": {
    "startCommand": "npm start",
    "healthcheckPath": "/api/health/ready",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
